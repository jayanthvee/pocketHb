numpy>=2.0
pandas>=2.0
pillow>=10.0
matplotlib>=3.8
scikit-learn>=1.4
torch>=2.3
torchvision>=0.18
timm>=1.0
huggingface_hub>=0.24
tqdm
gradio>=5.0
# audioop-lts is a backport of stdlib audioop, which Python 3.13 dropped;
# pydub (a Gradio transitive dep) imports it. Harmless on <3.13, required on 3.13.
audioop-lts; python_version>="3.13"
